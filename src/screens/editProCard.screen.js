import React from 'react';
import {Container} from '../assets/styleds'
import {Text} from 'react-native'

const EditProCard = ({navigation}) => {
    return(
        <Container>
            <Text>Pantalla de edición</Text>
        </Container>
    )
}
export default EditProCard;